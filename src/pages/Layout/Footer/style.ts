import styled from 'styled-components'
import { Layout } from 'antd'

const { Footer } = Layout

export const FooterStyle = styled(Footer)`
  padding-top: 12px;
  padding-bottom: 12px;
  text-align: center;
`
